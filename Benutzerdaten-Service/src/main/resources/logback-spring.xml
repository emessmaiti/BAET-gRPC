
    <configuration>
        <appender name="console" class="ch.qos.logback.core.ConsoleAppender">
            <encoder>
                <pattern>%d{yyyy-MM-dd HH:mm:ss} - %msg%n</pattern>
            </encoder>
        </appender>

        <root level="info">
            <appender-ref ref="console" />
        </root>

        <!-- You can also set specific log levels for packages or classes -->
        <logger name="de.th.koeln" level="debug" />
    </configuration>

